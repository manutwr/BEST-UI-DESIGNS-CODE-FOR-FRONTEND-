/* Reset and Base Setup */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: #05050a;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    perspective: 1000px;
}

/* Simple Cyber Grid Background */
.bg-grid {
    position: absolute;
    width: 100%;
    height: 100%;
    background-image: 
        linear-gradient(to right, rgba(0, 255, 242, 0.03) 1px, transparent 1px),
        linear-gradient(to bottom, rgba(0, 255, 242, 0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    z-index: 1;
}

/* 3D Wrapper Box */
.scene {
    position: relative;
    width: 300px;
    height: 400px;
    z-index: 2;
    display: flex;
    justify-content: center;
    align-items: center;
    transform-style: preserve-3d;
}

/* Robot Head Structural Shape */
.robot-head {
    position: relative;
    width: 260px;
    height: 320px;
    background: #0f1015;
    border: 2px solid #00fff2;
    /* Angled skull shape cut */
    clip-path: polygon(20% 0%, 80% 0%, 100% 20%, 100% 65%, 75% 100%, 25% 100%, 0% 65%, 0% 20%);
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: 40px;
    transform-style: preserve-3d;
    transition: transform 0.1s ease-out;
}

/* Neon Back Shadow */
.robot-glow {
    position: absolute;
    width: 264px;
    height: 324px;
    background: #00fff2;
    clip-path: polygon(20% 0%, 80% 0%, 100% 20%, 100% 65%, 75% 100%, 25% 100%, 0% 65%, 0% 20%);
    filter: blur(10px);
    opacity: 0.6;
    transition: transform 0.1s ease-out;
}

/* Left & Right Side Mechanical Ears */
.ear {
    position: absolute;
    top: 30%;
    width: 25px;
    height: 90px;
    background: #161922;
    border: 1px solid #ff0055;
    box-shadow: 0 0 10px rgba(255, 0, 85, 0.5);
    transition: transform 0.1s ease-out;
}
.ear-left {
    left: -15px;
    clip-path: polygon(0 20%, 100% 0, 100% 100%, 0 80%);
}
.ear-right {
    right: -15px;
    clip-path: polygon(0 0, 100% 20%, 100% 80%, 0 100%);
}

/* Forehead Plate Decoration */
.forehead {
    width: 140px;
    height: 30px;
    background: #1a1c23;
    border-bottom: 2px solid #00fff2;
    clip-path: polygon(15% 0%, 85% 0%, 100% 100%, 0% 100%);
    transform: translateZ(20px);
    margin-bottom: 20px;
}

/* Central Visor Panel */
.visor {
    width: 200px;
    height: 90px;
    background: #050508;
    border: 1px solid rgba(0, 255, 242, 0.4);
    clip-path: polygon(0% 0%, 100% 0%, 90% 100%, 10% 100%);
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: 0 20px;
    transform: translateZ(30px); /* Gives real physical 3D popup depth */
}

/* Target-Lock Diamond Eye Sockets */
.eye {
    width: 50px;
    height: 50px;
    background: #0b0c10;
    border: 2px solid #ff0055;
    box-shadow: 0 0 8px rgba(255, 0, 85, 0.3);
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    transform: rotate(45deg); 
}

/* Glowing Neon Blue Pupils */
.pupil {
    width: 18px;
    height: 18px;
    background: #00fff2;
    border-radius: 50%;
    box-shadow: 0 0 12px #00fff2, 0 0 20px #00fff2;
    transform: rotate(-45deg); /* Keeps the eye visually straight inside diamond */
    will-change: transform;
}

/* Cybernetic Jaw Structure */
.jaw {
    width: 110px;
    height: 70px;
    background: #111218;
    margin-top: 25px;
    clip-path: polygon(0% 0%, 100% 0%, 80% 100%, 20% 100%);
    border-top: 1px solid rgba(0, 255, 242, 0.2);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;
    transform: translateZ(15px);
}

/* Glowing Mouth Vent Grilles */
.line {
    width: 60px;
    height: 4px;
    background: #ff0055;
    box-shadow: 0 0 6px #ff0055;
    border-radius: 2px;
}
.line:nth-child(2) {
    width: 45px;
    background: #00fff2;
    box-shadow: 0 0 6px #00fff2;
}
.line:nth-child(3) {
    width: 25px;
}
